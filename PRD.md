# PRD：Isshin 极简 iOS 视频播放器

| 字段 | 内容 |
|------|------|
| 产品名称 | Isshin Player（工作目录：`isshin_ios_player`） |
| 版本 | v0.1（MVP） |
| 文档状态 | Draft |
| 目标平台 | iOS（iPhone 优先；iPad 可用但不单独优化） |
| 分发方式 | 自用，Xcode 真机安装；**不上架 App Store** |
| 关联需求 | `需求.txt` |

---

## 1. 背景与目标

### 1.1 背景
需要一个本地、零订阅成本的极简视频播放器：从系统相册导入视频，管理播放列表，完成播放控制（含倍速、画中画），并支持切后台/锁屏续播。

### 1.2 产品目标
- 打开 App → 导入相册视频（可多选）→ 列表管理 → 播放/暂停/进度/倍速/PiP，主路径清晰。
- 固定暗黑 UI，无主题切换，视觉对齐 Linear / Stripe / Vercel 极简风。
- 工程可维护：SwiftUI + 清晰分层。

### 1.3 非目标（本版不做）
- App Store / TestFlight 正式分发与审核材料
- 在线视频流、账号体系、云同步
- 字幕、投屏（AirPlay 可作为后续）、均衡器、手势快进（可后续）
- 亮色主题 / 主题切换
- 多工具侧栏壳（仅播放器产品，不扩展成工具箱）

### 1.4 成功标准（DoD）
- [ ] 可从相册多选视频并加入播放列表
- [ ] 列表点选切换、删除、当前项高亮；播完可自动下一首
- [ ] 播放 / 暂停 / 进度条 seek 正确
- [ ] 倍速切换立即生效，默认 1x
- [ ] 画中画可启动（系统支持时）；退出 PiP 后状态一致
- [ ] 切后台或锁屏后声音续播；控制中心可暂停/继续；有列表时可上下首
- [ ] Empty / Loading / Error 三态完整；有骨架或等效占位
- [ ] 全程暗黑，无切换入口
- [ ] 仓库含适用的 `.gitignore`，工程可在 Xcode 打开编译

---

## 2. 用户与场景

### 2.1 用户
本人自用；具备 iPhone + Mac + 免费 Apple ID；可接受证书周期重签。

### 2.2 核心场景

| ID | 场景 | 期望 |
|----|------|------|
| S1 | 首次打开 | 空态提示导入，一键打开选择器 |
| S2 | 多选导入 | 仅视频；加入播放列表；首条自动进入 Loading → Ready |
| S3 | 列表操作 | 点选切换当前片；左滑或按钮删除；当前项高亮 |
| S4 | 播放控制 | 播放/暂停；拖动进度 seek |
| S5 | 倍速 | 打开倍速菜单选档位，速率立即变，进度仍正确 |
| S6 | 画中画 | 点 PiP 进入系统小窗；可回 App 或关闭小窗 |
| S7 | 后台/锁屏 | **声音续播**；控制中心/锁屏可暂停、继续；有多条时可上一首/下一首 |
| S8 | 自动连播 | 当前片结束 → 播列表下一项（可配置，默认开） |
| S9 | 权限/失败 | Error 文案；可重试或重新导入；不白屏 |

---

## 3. 范围与优先级

| 优先级 | 能力 | MVP |
|--------|------|-----|
| P0 | 相册导入（仅 video，支持多选） | ✅ |
| P0 | 播放列表（展示 / 切换 / 删除 / 当前高亮） | ✅ |
| P0 | 播放 / 暂停 / 可拖动进度条 | ✅ |
| P0 | 倍速（0.5 / 0.75 / 1 / 1.25 / 1.5 / 2） | ✅ |
| P0 | 后台播放（`audio` + `.playback` + 远程控制） | ✅ |
| P0 | 画中画（`AVPictureInPictureController`） | ✅ |
| P0 | 固定暗黑 Theme + Loading / Empty / Error | ✅ |
| P1 | 播完自动下一首（默认开） | ✅ |
| P1 | 远程控制上一首 / 下一首 / 改进度 | ✅ |
| P2 | 列表拖拽排序、循环/单曲模式 | ❌ 可后续 |
| P2 | AirPlay / 字幕 | ❌ 延后 |

---

## 4. 功能需求详述

### 4.1 相册导入
- `PhotosPicker`（或 PHPicker），**仅 videos**；**允许多选**。
- 选中后异步加载；拷贝/安全转移到 App 沙盒可访问位置，写入播放列表模型（id、显示名、本地 URL、时长可选）。
- 导入后：若当前无播放项，自动加载列表第一项并**默认自动播放**；若已有播放项，仅追加列表，不打断当前播放（除非用户点选新项）。

### 4.2 播放列表
- 展示：标题（文件名或「视频 n」）、可选时长；当前项视觉高亮。
- 点选：切换 `AVPlayer` 资源，进入 Loading → Ready；默认自动播放。
- 删除：移除条目；若删的是当前项 → 停播并切到下一项（无下一项则 Empty/Paused 空画布）。
- 持久化：MVP **进程内即可**；杀进程清空可接受（后续可 UserDefaults/文件索引）。
- 空列表：Empty 引导导入。

### 4.3 播放器画布
- 主区域 `VideoPlayer` / `AVPlayerLayer` 封装；`resizeAspect`，近黑底。
- 无资源 Empty；加载中骨架；失败 Error。

### 4.4 播放 / 暂停
- SF Symbol：`play.fill` / `pause.fill`；与 `timeControlStatus` 同步。

### 4.5 进度条
- 当前时间 / 总时长 + Slider；`isSeeking` 防抖动；松手 seek；拖动前在播则 seek 后继续播。
- 时长未知时灰显。

### 4.6 倍速
- 档位：`0.5 / 0.75 / 1.0 / 1.25 / 1.5 / 2.0`，默认 `1.0`。
- 设置 `AVPlayer.rate`（播放中）或记录 `pendingRate`（暂停时，恢复播放用该速率）。
- UI：控件条入口弹出菜单或底部 sheet；当前档位有勾选态。
- 切集时：**保持当前倍速**（不重置为 1x）。

### 4.7 画中画（PiP）
- 使用 `AVPictureInPictureController`（需可 PiP 的 player layer / sample buffer 路径按系统要求配置）。
- `Info` / 能力：按需开启 PiP；控件在系统 `isPictureInPicturePossible` 为 true 时可用，否则禁用并勿崩溃。
- 行为：用户点 PiP → 进系统小窗；从 PiP 恢复 → 画面与进度对齐；关闭 PiP 且 App 在后台时按后台音频策略续声。
- 与后台：进后台可自动尝试 PiP（**可选**，默认 **不自动**，仅用户手势启动，避免打扰）；后台无 PiP 时至少保证音频续播。

### 4.8 后台播放
- `UIBackgroundModes` 含 `audio`。
- `AVAudioSession`：`category = .playback`，`mode = .moviePlayback`（或等价），`setActive(true)`。
- 后台 / 锁屏：**声音续播**；回前台进度与列表状态一致。
- `MPRemoteCommandCenter` + `MPNowPlayingInfoCenter`：play / pause；上一首 / 下一首（列表 ≥ 2 或有邻项时）；`changePlaybackPosition`（P1）。
- 来电等打断：**结束后保持暂停**（默认）。

### 4.9 自动连播
- 当前项 `AVPlayerItemDidPlayToEndTime` → 若开启自动下一首且存在下一项 → 加载并播放；否则停在结尾（显示重播或停在 paused at end）。
- MVP 默认 **开启**自动下一首；设置入口可极简（toggle 放列表旁或长按，P1）。

### 4.10 状态机（UI）

```
Empty ──导入──► Loading ──成功──► Ready(Playing|Paused|PiP)
                  │
                  └──失败──► Error ──重试/重导入──► Loading
Ready ──点选列表/下一首──► Loading …
Ready ──删光列表──► Empty
```

---

## 5. 信息架构与界面

### 5.1 页面结构（MVP）
```
┌──────────────────────────────────┐
│  Isshin          [导入] [列表外] │
├──────────────────────────────────┤
│                                  │
│         视频画布 / 空态           │
│                                  │
├──────────────────────────────────┤
│ ▶︎/❚❚   进度条    时间            │
│ 倍速    PiP     上/下一首（可选） │
├──────────────────────────────────┤
│ 播放列表                         │
│  • 视频 A          ← 当前        │
│  • 视频 B                        │
└──────────────────────────────────┘
```

- `NavigationStack`；导入在 toolbar；支持多选。
- 不做桌面左右分栏。

### 5.2 视觉与主题
- `preferredColorScheme(.dark)`；`Theme` 常量；无主题切换。
- SF Symbols only。

### 5.3 动效
- 短 `easeInOut`；列表切换可轻微过渡。

---

## 6. 技术架构

### 6.1 技术栈
Swift 5.9+ / SwiftUI / AVFoundation / AVKit / PhotosUI / MediaPlayer  
PiP：`AVPictureInPictureController`  
状态：`@Observable`  
无网络层（MVP）

### 6.2 目录建议
```
App/
Features/Player/          // View、ViewModel、Playlist、Controls
Core/
  Theme/
  Audio/                  // Session、Now Playing、Remote Command
  PiP/                    // PictureInPictureController 封装
  Permissions/
DesignSystem/
```

### 6.3 关键依赖与权限
| 项 | 说明 |
|----|------|
| 相册 | PhotosPicker 多选视频；按需 Usage Description |
| 后台 | `UIBackgroundModes` = `audio` |
| PiP | 系统支持检测；player layer 满足 PiP 要求 |
| 证书 | 免费 Apple ID 真机；约 7 天重签 |

### 6.4 工程杂项
- 根目录 `.gitignore` 已具备。
- Bundle 名「Isshin」；版本 0.1.0。
- 最低系统：**iOS 17+**。

---

## 7. 边界、异常与埋点

### 7.1 异常
| 情况 | 处理 |
|------|------|
| 取消选择器 | 保持原状 |
| 单条无法播放 | 该条 Error 或跳过并提示；列表其余可用 |
| 权限受限 | Error 文案 |
| seek 越界 | clamp |
| PiP 不可用 | 按钮禁用，无崩溃 |
| 音频会话失败 | 提示；尽量保留前台画面播放 |
| 列表删空 | Empty |

### 7.2 埋点
自用：**不做**分析埋点。

---

## 8. 验收用例（测试计划）

| # | 步骤 | 期望 |
|---|------|------|
| T1 | 冷启动 | Empty + 导入 |
| T2 | 多选 3 段视频导入 | 列表 3 条；首条可播 |
| T3 | 点列表第 2 条 | 切换成功并播放 |
| T4 | 删除当前项 | 切到合理下一项或空态 |
| T5 | 播放/暂停/seek | 行为正确 |
| T6 | 切到 1.5x | 速率立即变化；切集仍保持 1.5x |
| T7 | 启动 PiP | 小窗出现；进度续播；回 App 一致 |
| T8 | 播放中按 Home（无 PiP） | **声音续播**；控制中心可暂停 |
| T9 | 锁屏 | 续播；远程上一首/下一首可用 |
| T10 | 播完当前片 | 自动播下一首（默认开时） |
| T11 | 系统 Light 外观 | App 仍暗黑 |
| T12 | PiP 不支持设备 | 入口禁用，App 正常 |

---

## 9. 里程碑

| 阶段 | 交付 |
|------|------|
| M0 | 工程骨架 + 暗黑 Theme + `.gitignore` |
| M1 | 导入 + 单片播放/暂停/进度 |
| M2 | 播放列表 + 切集 + 自动下一首 |
| M3 | 倍速 + 后台播放 + 远程控制 |
| M4 | 画中画 + 三态打磨 + §8 真机验收 |

---

## 10. 开放问题

| ID | 问题 | 建议默认 |
|----|------|----------|
| Q1 | 新片/切集是否自动播放 | 是 |
| Q2 | 进后台是否自动进 PiP | 否（手动 PiP） |
| Q3 | 列表是否杀进程持久化 | MVP 否 |
| Q4 | 最低 iOS | 17+ |

---

## 11. 修订记录

| 日期 | 变更 |
|------|------|
| 2026-08-05 | 初版：由 `需求.txt` 展开为完整 PRD |
| 2026-08-05 | 纳入 P0：播放列表、倍速、画中画；强化后台播放与远程上下首 |
