# PRD：Isshin 极简 iOS 视频播放器

| 字段 | 内容 |
|------|------|
| 产品名称 | Isshin Player（工作目录：`isshin_ios_player`） |
| 版本 | v0.1（MVP） |
| 文档状态 | Draft |
| 目标平台 | iOS（iPhone 优先；iPad 可用但不单独优化） |
| 分发方式 | 自用，Xcode 真机安装；**不上架 App Store** |
| 关联需求 | `需求.txt` |

---

## 1. 背景与目标

### 1.1 背景
需要一个本地、零订阅成本的极简视频播放器：从系统相册导入视频，完成基础播放控制，并支持切后台/锁屏时继续出声（音频会话保持）。

### 1.2 产品目标
- 打开 App → 导入相册视频 → 播放/暂停/拖动进度，流程在 **3 步内**完成。
- 固定暗黑 UI，无主题切换，视觉对齐 Linear / Stripe / Vercel 极简风。
- 工程可维护：SwiftUI + 清晰分层，便于后续加「多工具」壳。

### 1.3 非目标（本版不做）
- App Store / TestFlight 正式分发与审核材料
- 在线视频流、账号体系、云同步
- 播放列表、字幕、倍速、画中画、投屏、均衡器
- 亮色主题 / 主题切换
- 多工具侧栏（仅预留结构，MVP 单播放器页）

### 1.4 成功标准（DoD）
- [ ] 可从相册仅选视频并加载到播放器
- [ ] 播放 / 暂停 / 进度条 seek 行为正确
- [ ] 切后台或锁屏后音频可续播；控制中心可暂停/继续
- [ ] Empty / Loading / Error 三态完整；有骨架或等效占位
- [ ] 全程暗黑，无切换入口
- [ ] 仓库含适用的 `.gitignore`，工程可在 Xcode 打开编译（真机需本机证书）

---

## 2. 用户与场景

### 2.1 用户
本人自用；具备 iPhone + Mac + 免费 Apple ID；可接受证书周期重签。

### 2.2 核心场景

| ID | 场景 | 期望 |
|----|------|------|
| S1 | 首次打开 | 空态提示「从相册导入视频」，一键打开选择器 |
| S2 | 导入视频 | 仅展示视频资源；选中后进入 Loading，就绪后显示画面与控件 |
| S3 | 播放控制 | 点播放/暂停；拖动进度条 seek，松手后从新位置播放 |
| S4 | 后台/锁屏 | 视频画面可停在后台，**声音继续**；系统媒体控件可暂停 |
| S5 | 权限拒绝 | 明确 Error/引导文案，可再次尝试或去设置（按系统能力） |
| S6 | 加载失败 | Error 态 + 可重试/重新导入，不白屏 |

---

## 3. 范围与优先级

| 优先级 | 能力 | MVP |
|--------|------|-----|
| P0 | 相册导入（仅 video） | ✅ |
| P0 | 播放 / 暂停 | ✅ |
| P0 | 可拖动进度条 | ✅ |
| P0 | 后台音频（`audio` background mode + `.playback` session） | ✅ |
| P0 | 固定暗黑 Theme | ✅ |
| P0 | Loading / Empty / Error + 骨架/占位 | ✅ |
| P1 | 控制中心 / 锁屏媒体信息（标题可用文件名或「Isshin」） | ✅ 建议随后台音频一并做 |
| P2 | 换片后自动替换当前资源 | ✅ 建议 MVP 支持「再次导入即替换」 |
| P3 | 多工具入口壳、播放列表、手势快进等 | ❌ 延后 |

---

## 4. 功能需求详述

### 4.1 相册导入
- 使用 `PhotosPicker`（或 PHPicker），`filter` 限定 **videos only**。
- 选中后异步加载可播放资源（安全转移/拷贝到 App 可访问位置，避免相册权限生命周期问题）。
- 再次导入：替换当前 `AVPlayer` 资源；若正在播放则停在新资源就绪策略（建议：就绪后自动播放或保持暂停，**默认自动播放**，需在实现中统一）。

### 4.2 播放器画布
- 上方主区域展示视频画面（`VideoPlayer` / `AVPlayerLayer` 封装）。
- 无资源：Empty；加载中：骨架或 redacted；失败：Error。
- 画面默认 `resizeAspect`（letterbox），背景近黑。

### 4.3 播放 / 暂停
- 主按钮 SF Symbol：`play.fill` / `pause.fill`。
- 点击切换；与 `AVPlayer.timeControlStatus` 同步，避免状态漂移。

### 4.4 进度条
- 显示当前时间 / 总时长（可选精简为一条 Slider + 时间标签）。
- 拖动中暂停时间观测更新或标记 `isSeeking`，松手后 `seek` 到目标，完成后恢复拖动前的播/停意图（拖动前在播则继续播）。
- 总时长未知时禁用或灰显 Slider。

### 4.5 后台音频
- `Info.plist`：`UIBackgroundModes` 含 `audio`。
- 启动时配置 `AVAudioSession`：`category = .playback`，`mode` 按视频合理选择（如 `.moviePlayback`），并 `setActive(true)`。
- 进入后台：允许继续播放音频；回前台画面与进度对齐。
- 接入 `MPRemoteCommandCenter`：至少 play / pause（进度建议支持 changePlaybackPosition，P1）。
- 打断（来电等）：遵循系统打断通知，结束后按策略恢复（可暂停不自动恢复，避免吵用户——**默认打断后保持暂停**）。

### 4.6 状态机（UI）

```
Empty ──导入──► Loading ──成功──► Ready(Playing|Paused)
                  │
                  └──失败──► Error ──重试/重导入──► Loading
Ready ──再次导入──► Loading …
```

---

## 5. 信息架构与界面

### 5.1 页面结构（MVP 单页）
```
┌─────────────────────────────┐
│  导航栏：Isshin / 导入按钮   │
├─────────────────────────────┤
│                             │
│       视频画布 / 空态        │
│                             │
├─────────────────────────────┤
│  [播放/暂停]  进度条  时间   │
└─────────────────────────────┘
```

- 导航：可用 `NavigationStack`；导入入口放 toolbar。
- **不做**左右分栏；多工具壳列为后续。

### 5.2 视觉与主题
- 强制 `preferredColorScheme(.dark)`。
- `Theme` 常量化：背景近黑、表面 Zinc、主文字白/浅灰、控件低饱和。
- **无**主题切换 UI 与设置项。
- 图标：SF Symbols only。

### 5.3 动效
- 控件 `easeInOut` 短过渡；不引入复杂叙事动效库。

---

## 6. 技术架构

### 6.1 技术栈
Swift 5.9+ / SwiftUI / AVFoundation / AVKit / PhotosUI  
状态：`@Observable`  
图标：SF Symbols  
无网络层（MVP）

### 6.2 目录建议
```
App/                 // @main、根 View、色域锁定
Features/Player/     // PlayerView、PlayerViewModel、控件
Core/
  Theme/             // 固定暗黑色板
  Audio/             // Session、Remote Command
  Permissions/       // 文案与引导（如需要）
DesignSystem/        // 骨架、空态、错误组件
```

### 6.3 关键依赖与权限
| 项 | 说明 |
|----|------|
| 相册 | PhotosPicker 选视频；按需 `NSPhotoLibraryUsageDescription`（若走 PHAsset 完整库访问再补） |
| 后台 | `UIBackgroundModes` = `audio` |
| 证书 | 免费 Apple ID 真机；约 7 天重签 |

### 6.4 工程杂项
- 根目录 `.gitignore`：通用 + Xcode/iOS 忽略项。
- Bundle 显示名可用「Isshin」；版本 0.1.0。

---

## 7. 边界、异常与埋点

### 7.1 异常
| 情况 | 处理 |
|------|------|
| 用户取消选择器 | 保持原状态 |
| 资源无法播放 / 格式异常 | Error + 重新导入 |
| 权限受限 | Error 文案说明 |
| seek 越界 | clamp 到 `[0, duration]` |
| 音频会话激活失败 | Error 或 Toast 级提示，仍可尝试播画面 |

### 7.2 埋点
自用 App：**不做**分析埋点。

---

## 8. 验收用例（测试计划）

| # | 步骤 | 期望 |
|---|------|------|
| T1 | 冷启动无历史视频 | Empty，可见导入 |
| T2 | 导入一段相册视频 | Loading → 出画，可播 |
| T3 | 播放中点暂停再播放 | 状态与画面一致 |
| T4 | 拖动进度到中部松开 | 从该位置继续 |
| T5 | 播放中按 Home | 声音续播；控制中心可暂停 |
| T6 | 锁屏 | 声音续播；锁屏控件可用 |
| T7 | 来电打断后挂断 | 保持暂停（按 4.5 默认） |
| T8 | 导入损坏/不支持文件 | Error，可再导入 |
| T9 | 系统外观为 Light | App 仍为暗黑 |

---

## 9. 里程碑

| 阶段 | 交付 |
|------|------|
| M0 | Xcode 工程骨架 + Theme 暗黑 + `.gitignore` |
| M1 | 导入 + 播放/暂停 + 进度 |
| M2 | 后台音频 + 远程控制 |
| M3 | 三态/骨架打磨 + 真机验收按 §8 |

---

## 10. 开放问题

| ID | 问题 | 建议默认 |
|----|------|----------|
| Q1 | 新视频加载完成后是否自动播放 | 是 |
| Q2 | 是否显示视频文件名 | MVP 导航副标题可选显示 |
| Q3 | 最低 iOS 版本 | iOS 17+（对齐 PhotosPicker / Observation） |

---

## 11. 修订记录

| 日期 | 变更 |
|------|------|
| 2026-08-05 | 初版：由 `需求.txt` 展开为完整 PRD |
